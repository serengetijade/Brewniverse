import React from 'react';
import { Routes, Route } from 'react-router-dom';

function Calculator() {
  return (
    <Routes>
      {/*<Route index element={<CalculatorList />} />*/}
      {/*<Route path="new" element={<CalculatorForm />} />*/}
      {/*<Route path=":id" element={<CalculatorDetail />} />*/}
      {/*<Route path=":id/edit" element={<CalculatorForm />} />*/}
    </Routes>
  );
}

export default Calculator;

