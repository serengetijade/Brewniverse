export const Validation = {
    InputMaxLength: 200,

    TextareaMaxLength: 1000,

    NumberMin: 0,

    // EMAIL_REGEX: /^[^\s@]+@[^\s@]+\.[^\s@]+$/,
    // PHONE_REGEX: /^\d{10}$/,
};

export default Validation;